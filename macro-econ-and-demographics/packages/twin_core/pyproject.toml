[project]
name = "twin-core"
version = "0.1.0"
description = "Artifact contract shared by twin training and serving."
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.11",
]

[build-system]
requires = ["uv_build>=0.12.9,<0.13"]
build-backend = "uv_build"
